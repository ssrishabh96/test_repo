import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  issueItemView: {
    padding: 10,
    flexDirection: 'row',
    justifyContent: 'space-between',
    alignItems: 'center',
  },
  textDescription: {
    fontSize: 18,
    maxWidth: 300,
  },
});
