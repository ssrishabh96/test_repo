import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    // minHeight: 50,
    flexDirection: 'row',
  },
  content: {
    flex: 1,
    flexWrap: 'wrap',
  },
});

export default styles;
